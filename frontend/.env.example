# API requests always go through the Next.js rewrite proxy (/api → backend).
# Set API_UPSTREAM in your Vercel environment variables to point to Railway:
# API_UPSTREAM=https://your-production-url.com
#
# For local development, next.config.ts defaults to http://localhost:8008
